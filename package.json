{
  "name": "pratyush-liftz-site",
  "version": "1.0.0",
  "private": true,
  "description": "Pratyush Liftz — static site (frontend/), admin console (admin/) and content build.",
  "scripts": {
    "build": "node build/build.mjs build",
    "extract": "node build/build.mjs extract",
    "preview": "npm run build && python3 -m http.server 4173 --directory dist",
    "test": "node test/results-lightbox.test.mjs && node test/form-flow.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
